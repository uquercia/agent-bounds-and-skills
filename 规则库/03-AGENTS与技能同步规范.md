# AGENTS 与技能同步规范

## 适用场景
- 修改全局 `AGENTS.md`
- 修改 `skills`
- 修改规则库文档
- 调整 `G:\codex_skills\存档`

## 约束
- 只要修改全局 `AGENTS.md` 或 `skills` 相关内容，都要同步到 `G:\codex_skills` 对应文件或目录，保持两处一致。
- 规则库作为全局约束延伸，也应同步到 `G:\codex_skills` 中对应目录，避免本地和技能仓库规则分叉。
- 同步前先确认变更范围，仅同步规则、技能、说明文档和必要的存档文件，不带入无关临时文件。
