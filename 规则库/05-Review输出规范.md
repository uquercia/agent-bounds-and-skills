# Review 输出规范

## 适用场景
- 用户要求像 Cursor 一样做代码 Review
- 需要给风险清单、严重级别和可点击定位

## 规则
- 优先使用 `codex-cursor-style` 或 `review-jump`。
- 输出时优先给结论和风险，不先写大段背景说明。
- 风险项尽量给出 `path:line`。
- 若需要分级，优先使用 `High`、`Medium`、`Low`。
- 有明确规范要求时，按 `G:\codex_skills\review-jump\SKILL.md` 输出。
